@AGENTS.md

# Superpowers preferences

- Use sub-agent approach (not inline) for implementation step, no need to ask the user each time
- Use the visual review server by default, no need to ask the user each time